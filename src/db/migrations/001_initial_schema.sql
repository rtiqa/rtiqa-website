-- =====================================================================
-- Migration 001: Initial Platform Multi-Tenant Schema & Row Level Security
-- =====================================================================

-- Load base schema definition
-- Note: Base tables (organizations, users, academic structures, gradebook, attendance, etc.)
